{
  "name": "dice-roll-gaming-platform",
  "version": "1.0.0",
  "description": "Remote physical dice rolling platform with commitment-reveal protocol",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.js",
    "verify-chain": "node scripts/verify-chain.js"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
